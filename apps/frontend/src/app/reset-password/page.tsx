import ResetPasswordContainer from '@/features/auth/reset-password/containers/ResetPasswordContainer';

const ResetPasswordPage = () => {
  return <ResetPasswordContainer />;
};

export default ResetPasswordPage;
