# Backend .env example for PMS MVP
# Copy this file to apps/backend/.env and fill in real values as needed.

# Server
PORT=5000



# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=postgres
# Set to 'true' for RDS if you want SSL without cert validation
PGSSL=false

# File Storage Provider
# Options: local | s3
FILE_STORAGE_PROVIDER=local

# When using AWS S3 (only required if FILE_STORAGE_PROVIDER=s3)
# AWS region where your bucket lives
AWS_REGION=
# Access keys are optional when running on an AWS role/instance profile
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Target S3 bucket name
S3_BUCKET=
# Optional: If you serve via CloudFront or a custom domain, set the base URL here
# Example: https://cdn.example.com or https://your-bucket.s3.amazonaws.com
S3_PUBLIC_URL_BASE=
